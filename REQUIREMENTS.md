# img2braille 要件定義 v1.0

## 1. 目的・背景
画像を誰でもコピペできるテキストの点字アスキーアートに変換する。
SNS・掲示板・チャット等の等幅テキスト面に画像の雰囲気を貼れることを目的とする。

## 2. スコープ
- IN: 画像ファイル入力 → 点字アスキーアート出力（コピー / TXT保存）
- OUT（v1対象外）: カラー点字、動画/GIFアニメ変換、サーバ保存・共有URL、OCR・代替テキスト自動生成

## 3. 用語
- セル: 点字1文字（2×4ドット）。U+2800〜U+28FF の64…256パターン。
- ドット解像度: 出力幅W文字 → 2W × 4H ドット。

## 4. 機能要件
| ID | 要件 | 備考 |
|---|---|---|
| FR-01 | 画像入力（選択 / D&D / ペースト） | PNG/JPEG/GIF/WebP/BMP/SVG |
| FR-02 | グレースケール＋セル8段階濃淡（1文字=2×4ドット、ドット数1〜8で濃度表現、空セルU+2800不使用） | 反転対応。反転の初期値は `prefers-color-scheme` 連動（ダーク=ON・ライト=OFF）。手動変更後はテーマ切替に追従せず、リセットで連動に戻る |
| FR-03 | ディザ3種（FS / Atkinson / なし） | セル単位の誤差拡散。既定 FS |
| FR-04 | 画質調整（明るさ・コントラスト・ガンマ） | リアルタイム再変換 |
| FR-05 | 出力幅は数値入力で自由指定（丸め込みなし） | 上限はゲージ（スライダ）が持ち、ロード・リサイズ時に「画面幅÷最小1.5px÷セル幅」で再計算。ラベル・ゲージ位置は実効幅に追従。総文字数400万のメモリガード時は縦横比を保ったまま均等縮小 |
| FR-06 | ワンクリックコピー（Clipboard API＋fallback） | 全セルにドット1〜8個を含むためフォントの送り幅が揃いコピペ崩れなし |
| FR-07 | TXTダウンロード（UTF-8） | `braille.txt` |
| FR-08 | サンプル画像で無入力試用 | Canvas生成 |
| FR-09 | 入力プレビュー・出力メタ表示 | W×H・ドット数・字数 |
| FR-10 | パラメータリセット | 全スライダ・ディザ・反転・表示サイズ自動を初期値（幅70/明暗コントラスト0/ガンマ1.00/補正1.00/FS/サイズ11px・自動ON、反転はテーマ連動値）に戻し即時再変換 |
| FR-11 | 出力幅連動の表示サイズ自動調整 | 幅に収まる最大px（1.5〜200px・0.5px刻み）に自動設定。ブロック自体はスクロールせずページが縦に伸びる。手動スライダ操作で自動OFF→指定px固定。リセットで自動ON復帰。コピー/TXT内容は不変（表示のみ） |
| FR-12 | 全画面ビューワ | オーバーレイに展開し画面（幅・高さ両方）に収まる最大フォントで表示（1.5〜200px）。開いたまま再変換しても同期。Esc・閉じるボタンで終了、コピー可 |
## 5. 変換仕様
1. 透明は白合成 → 目標ドット解像度に `drawImage` 一括縮小（`imageSmoothingQuality: high`）。
2. 輝度 `Y = 0.299R + 0.587G + 0.114B` → contrast/brightness → clamp → `pow(v, gamma)` → 反転。
3. 高さ `H = round(W × ih/iw × R × 補正)`。R は表示フォント実測のセル縦横比（advance/line-height、約0.5）。全セルにドットを含むためadvance実測は安定。
4. セル濃度: セル2×4ドットの平均輝度をドット数1〜8に量子化（暗いほど多）。ドットは中央2行から外側へ成長（ORDER順）。FS/Atkinsonはセル単位の誤差拡散、`なし`は直接量子化。
5. 空白セル U+2800・U+0020 はどちらも出力しない（行末trim不要・コピペ崩れ防止）。ドット入り文字は全て同じ送り幅。

## 6. UI要件
- 日本語UI、単一ページ、ライブ再変換（input イベント駆動）。
- 出力は `white-space: pre`＋等幅、`user-select: all`、ブロック自体にスクロールバーなし（幅フィット＋ページ縦伸び）。
- 状態: 未入力 / 変換済（コピー・保存活性）/ エラー行表示。

## 7. 非機能要件
| ID | 要件 |
|---|---|
| NFR-01 | GitHub Pages の静的ホスティングのみで動作（ビルド不要・外部CDN不要・単一 `index.html`） |
| NFR-02 | プライバシ: 画像は端末内処理、ネットワーク送信なし |
| NFR-03 | 性能: 上限なし設計。テキスト総文字数400万（約8MB）をメモリガードとし、超過時は縦横比維持でW・Hとも縮小。ラスタはタイル分割描画。巨大変換は非同期＋進捗表示＋世代トークンで連打キャンセル可。常用域は再変換 <300ms 目安 |
| NFR-04 | 対応: 現行 Chrome/Edge/Firefox/Safari、ファイル・D&D・Clipboard API 欠落時は fallback/明確な文言 |
| NFR-05 | 可搬性: UTF-8 テキスト出力が等幅フォント面で崩れない（ドット入り文字のみ・U+2800不使用） |

## 8. 受け入れ基準
- [ ] サンプル画像→70文字幅でそれらしい濃淡アートが出る
- [ ] コピー→メモ帳等に貼って目視一致（全セル等幅で波打ちなし）
- [ ] 白黒反転・ディザ切替が即時反映される
- [ ] 透過PNGが黒潰れせず白背景扱いになる
- [ ] `file://` 直開きでも主要動線が動く（ES module不使用の必然）

## 9. GitHub Pages 運用
- `main` ブランチ `/ (root)` を Pages ソースに指定（Settings → Pages → Deploy from a branch）。
- カスタムビルド・Actions 不要。`.nojekyll` 同梱で `_` 始まり対策。
- 公開URL: `https://<user>.github.io/img2braille/`

## 10. 将来拡張（non-goal）
URL入力（CORS検討要）、ANSIカラー併用出力、GIFアニメのコマ出力、PWAオフライン化。
